 
{
  "tag-pair": true,  // Ensure tags are correctly paired
  "attr-value-double-quotes": true,  // Ensure attributes are enclosed in double quotes
  "doctype-first": true,  // Ensure doctype is declared first
  "id-unique": true,  // Ensure IDs are unique
  "img-alt-require": true  // Ensure that all images have an alt attribute
}
